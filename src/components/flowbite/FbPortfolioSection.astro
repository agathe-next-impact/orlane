---
import type { WPACFFbPortfolioSection } from '../../types/wp';

interface Props extends WPACFFbPortfolioSection {}

const { heading, description, projects = [], variant = 'cards', ctaText, ctaUrl } = Astro.props;
---

{variant === 'cards' && (
  <section class="bg-canvas">
    <div class="py-16 px-4 mx-auto max-w-screen-xl lg:py-16 lg:px-6">
      {(heading || description) && (
        <div class="mx-auto max-w-screen-sm text-center mb-8 lg:mb-12">
          {heading && (
            <h2 class="mb-4 text-3xl lg:text-4xl tracking-tight font-extrabold text-ink font-heading">{heading}</h2>
          )}
          {description && (
            <p class="font-light text-ink/70 sm:text-lg">{description}</p>
          )}
        </div>
      )}
      <div class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3">
        {projects.map((project) => (
          <div class="group relative overflow-hidden rounded-lg shadow-md hover:shadow-xl transition-shadow">
            {project.image && (
              <img class="w-full h-48 object-cover group-hover:scale-105 transition-transform duration-300" src={project.image.sourceUrl} alt={project.image.altText || project.title || ''} loading="lazy" />
            )}
            <div class="absolute inset-0 bg-gradient-to-t from-taupe/80 to-transparent flex flex-col justify-end p-6">
              {project.category && (
                <span class="text-xs font-medium accent-text uppercase mb-1">{project.category}</span>
              )}
              {project.title && (
                <h3 class="text-xl font-bold text-canvas mb-1">
                  {project.url ? (
                    <a href={project.url} class="accent-text-hover transition-colors">{project.title}</a>
                  ) : project.title}
                </h3>
              )}
              {project.description && (
                <p class="text-sm text-surface/80 line-clamp-2">{project.description}</p>
              )}
            </div>
          </div>
        ))}
      </div>
      {ctaText && ctaUrl && (
        <div class="text-center mt-12">
          <a href={ctaUrl} class="btn-primary text-canvas accent-bg accent-bg-hover focus:ring-4 focus:outline-none accent-ring">
            {ctaText}
          </a>
        </div>
      )}
    </div>
  </section>
)}

{variant === 'minimal' && (
  <section class="bg-canvas">
    <div class="py-16 px-4 mx-auto max-w-screen-xl lg:py-16 lg:px-6">
      {heading && (
        <h2 class="mb-8 text-3xl lg:text-4xl tracking-tight font-extrabold text-ink font-heading">{heading}</h2>
      )}
      <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
        {projects.map((project) => (
          <a href={project.url || '#'} class="group block">
            {project.image && (
              <img class="w-full h-48 object-cover rounded-lg mb-4 group-hover:opacity-90 transition-opacity" src={project.image.sourceUrl} alt={project.image.altText || project.title || ''} loading="lazy" />
            )}
            {project.category && (
              <span class="text-xs font-medium accent-text uppercase">{project.category}</span>
            )}
            {project.title && (
              <h3 class="text-lg font-bold text-ink accent-text-hover transition-colors">{project.title}</h3>
            )}
            {project.description && (
              <p class="mt-1 text-sm text-ink/70">{project.description}</p>
            )}
          </a>
        ))}
      </div>
    </div>
  </section>
)}

{variant === 'masonry' && (
  <section class="bg-canvas">
    <div class="py-16 px-4 mx-auto max-w-screen-xl lg:py-16 lg:px-6">
      {(heading || description) && (
        <div class="mx-auto max-w-screen-sm text-center mb-8 lg:mb-12">
          {heading && <h2 class="mb-4 text-3xl lg:text-4xl tracking-tight font-extrabold text-ink font-heading">{heading}</h2>}
          {description && <p class="font-light text-ink/70 sm:text-lg">{description}</p>}
        </div>
      )}
      <div class="columns-1 sm:columns-2 lg:columns-3 gap-6 space-y-6">
        {projects.map((project) => (
          <div class="break-inside-avoid group relative overflow-hidden rounded-lg shadow-md hover:shadow-xl transition-shadow">
            {project.image && (
              <img class="w-full object-cover" src={project.image.sourceUrl} alt={project.image.altText || project.title || ''} loading="lazy" />
            )}
            <div class="p-4 bg-canvas border border-t-0 border-surface rounded-b-lg">
              {project.category && (
                <span class="text-xs font-medium accent-text uppercase">{project.category}</span>
              )}
              {project.title && (
                <h3 class="text-lg font-bold text-ink">
                  {project.url ? <a href={project.url} class="accent-text-hover transition-colors">{project.title}</a> : project.title}
                </h3>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>
)}
