---
import type { WPACFFbAuthForm } from '../../types/wp';

interface Props extends WPACFFbAuthForm {}

const { heading, description, formAction, variant = 'login', logo, forgotPasswordUrl, registerUrl, loginUrl, privacyUrl, termsUrl } = Astro.props;
---

{variant === 'login' && (
  <section class="bg-surface/30">
    <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
      {logo && (
        <a href="/" class="flex items-center mb-6">
          <img class="w-8 h-8 mr-2" src={logo.sourceUrl} alt={logo.altText || ''} />
        </a>
      )}
      <div class="w-full bg-canvas rounded-lg shadow-md md:mt-0 sm:max-w-md xl:p-0 border border-surface">
        <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
          {heading && (
            <h1 class="text-xl font-bold leading-tight tracking-tight text-ink md:text-2xl font-heading">{heading}</h1>
          )}
          <form class="space-y-4 md:space-y-6" action={formAction || '#'} method="POST">
            <div>
              <label for="email" class="block mb-2 text-sm font-medium text-ink">Email</label>
              <input type="email" name="email" id="email" class="input-themed bg-surface/20 border border-surface text-ink accent-ring accent-border block w-full p-2.5" placeholder="nom@exemple.com" required />
            </div>
            <div>
              <label for="password" class="block mb-2 text-sm font-medium text-ink">Mot de passe</label>
              <input type="password" name="password" id="password" class="input-themed bg-surface/20 border border-surface text-ink accent-ring accent-border block w-full p-2.5" placeholder="••••••••" required />
            </div>
            <div class="flex items-center justify-between">
              <div class="flex items-start">
                <div class="flex items-center h-5">
                  <input id="remember" aria-describedby="remember" type="checkbox" class="w-4 h-4 border border-surface rounded bg-surface/20 focus:ring-3 accent-ring" />
                </div>
                <div class="ml-3 text-sm">
                  <label for="remember" class="text-ink/70">Se souvenir de moi</label>
                </div>
              </div>
              {forgotPasswordUrl && (
                <a href={forgotPasswordUrl} class="text-sm font-medium accent-text hover:underline">Mot de passe oublié ?</a>
              )}
            </div>
            <button type="submit" class="btn-primary w-full text-canvas accent-bg accent-bg-hover focus:ring-4 focus:outline-none accent-ring">Se connecter</button>
            {registerUrl && (
              <p class="text-sm font-light text-ink/70">
                Pas encore de compte ? <a href={registerUrl} class="font-medium accent-text hover:underline">S'inscrire</a>
              </p>
            )}
          </form>
        </div>
      </div>
    </div>
  </section>
)}

{variant === 'register' && (
  <section class="bg-surface/30">
    <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
      {logo && (
        <a href="/" class="flex items-center mb-6">
          <img class="w-8 h-8 mr-2" src={logo.sourceUrl} alt={logo.altText || ''} />
        </a>
      )}
      <div class="w-full bg-canvas rounded-lg shadow-md sm:max-w-md xl:p-0 border border-surface">
        <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
          {heading && (
            <h1 class="text-xl font-bold leading-tight tracking-tight text-ink md:text-2xl font-heading">{heading}</h1>
          )}
          <form class="space-y-4 md:space-y-6" action={formAction || '#'} method="POST">
            <div>
              <label for="reg-name" class="block mb-2 text-sm font-medium text-ink">Nom complet</label>
              <input type="text" name="name" id="reg-name" class="input-themed bg-surface/20 border border-surface text-ink accent-ring accent-border block w-full p-2.5" placeholder="Votre nom" required />
            </div>
            <div>
              <label for="reg-email" class="block mb-2 text-sm font-medium text-ink">Email</label>
              <input type="email" name="email" id="reg-email" class="input-themed bg-surface/20 border border-surface text-ink accent-ring accent-border block w-full p-2.5" placeholder="nom@exemple.com" required />
            </div>
            <div>
              <label for="reg-password" class="block mb-2 text-sm font-medium text-ink">Mot de passe</label>
              <input type="password" name="password" id="reg-password" class="input-themed bg-surface/20 border border-surface text-ink accent-ring accent-border block w-full p-2.5" placeholder="••••••••" required />
            </div>
            <div>
              <label for="reg-confirm" class="block mb-2 text-sm font-medium text-ink">Confirmer le mot de passe</label>
              <input type="password" name="confirm-password" id="reg-confirm" class="input-themed bg-surface/20 border border-surface text-ink accent-ring accent-border block w-full p-2.5" placeholder="••••••••" required />
            </div>
            {(termsUrl || privacyUrl) && (
              <div class="flex items-start">
                <div class="flex items-center h-5">
                  <input id="terms" type="checkbox" class="w-4 h-4 border border-surface rounded bg-surface/20 focus:ring-3 accent-ring" required />
                </div>
                <div class="ml-3 text-sm">
                  <label for="terms" class="font-light text-ink/70">
                    J'accepte les {termsUrl && <a href={termsUrl} class="font-medium accent-text hover:underline">CGU</a>}
                    {termsUrl && privacyUrl && ' et la '}
                    {privacyUrl && <a href={privacyUrl} class="font-medium accent-text hover:underline">politique de confidentialité</a>}
                  </label>
                </div>
              </div>
            )}
            <button type="submit" class="btn-primary w-full text-canvas accent-bg accent-bg-hover focus:ring-4 focus:outline-none accent-ring">Créer un compte</button>
            {loginUrl && (
              <p class="text-sm font-light text-ink/70">
                Déjà un compte ? <a href={loginUrl} class="font-medium accent-text hover:underline">Se connecter</a>
              </p>
            )}
          </form>
        </div>
      </div>
    </div>
  </section>
)}

{variant === 'reset_password' && (
  <section class="bg-surface/30">
    <div class="flex flex-col items-center justify-center px-6 py-8 mx-auto md:h-screen lg:py-0">
      {logo && (
        <a href="/" class="flex items-center mb-6">
          <img class="w-8 h-8 mr-2" src={logo.sourceUrl} alt={logo.altText || ''} />
        </a>
      )}
      <div class="w-full bg-canvas rounded-lg shadow-md sm:max-w-md xl:p-0 border border-surface">
        <div class="p-6 space-y-4 md:space-y-6 sm:p-8">
          <h1 class="text-xl font-bold leading-tight tracking-tight text-ink md:text-2xl font-heading">
            {heading || 'Réinitialiser le mot de passe'}
          </h1>
          {description && (
            <p class="text-sm text-ink/70">{description}</p>
          )}
          <form class="space-y-4 md:space-y-6" action={formAction || '#'} method="POST">
            <div>
              <label for="reset-email" class="block mb-2 text-sm font-medium text-ink">Votre email</label>
              <input type="email" name="email" id="reset-email" class="input-themed bg-surface/20 border border-surface text-ink accent-ring accent-border block w-full p-2.5" placeholder="nom@exemple.com" required />
            </div>
            <button type="submit" class="btn-primary w-full text-canvas accent-bg accent-bg-hover focus:ring-4 focus:outline-none accent-ring">Envoyer le lien</button>
            {loginUrl && (
              <p class="text-sm font-light text-ink/70 text-center">
                <a href={loginUrl} class="font-medium accent-text hover:underline">Retour à la connexion</a>
              </p>
            )}
          </form>
        </div>
      </div>
    </div>
  </section>
)}
